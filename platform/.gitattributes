# Shell scripts must keep LF endings — they run inside Linux containers and a
# CRLF shebang breaks them. Force LF regardless of the committer's platform.
*.sh text eol=lf
cron/entrypoint.sh text eol=lf
