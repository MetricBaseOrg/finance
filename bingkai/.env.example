# Neon Postgres (Vercel Marketplace). Without it the app runs in file-backed dev mode.
DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"
# Public origin, used for share links and QR codes.
NEXT_PUBLIC_SITE_URL="https://bingkai.metricbase.org"
